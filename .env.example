PORT=3000
JWT_SECRET=change-me-in-production-use-long-random-string
# JWT_EXPIRES_IN omitted = tokens never expire

# Cron: generació automàtica de comandes setmanals (cada dia a les 8:00)
# CRON_WEEKLY_ORDERS_ENABLED=true
